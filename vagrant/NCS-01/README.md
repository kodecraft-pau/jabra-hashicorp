# Commands

```
vagrant up
vagrant ssh
sudo service nomad status
sudo service consul status
```

VM default login details:
* user: `vagrant`
* password: `vagrant`

### nomad dashboard
* http://localhost:4646

### consul dashboard
* http://localhost:8500/